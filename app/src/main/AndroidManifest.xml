<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

<application
        android:allowBackup="true"
        android:label="DentalApp"
        tools:replace="android:label"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DentalApp">

        <service
            android:name=".services.MyFirebaseService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>

    <activity
            android:name=".ui.auth.RegisterActivity"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.home.HomeActivity"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.profile.EditProfileActivity"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.info.ClinicInfoActivity"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.appointments.AppointmentActivity"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.appointments.MyAppointmentsActivity"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.chat.ChatBotActivity"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.auth.ForgotPasswordActivity"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.promotions.PromotionsActivity"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.medical.MedicalRecordActivity"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.admin.AdminPromotionsActivity"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.admin.AdminMainActivity"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.admin.EditPromotionActivity"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.auth.LoginActivity"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

</application>

</manifest>